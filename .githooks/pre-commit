#!/bin/sh
# Block commits directly on main.
branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"
if [ "$branch" = "main" ]; then
  echo "ERROR: Direct commits to main are blocked. Create a feature branch first." >&2
  exit 1
fi
